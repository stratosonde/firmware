/* Host-test stub (#57): atgm336h.c includes usart_if.h but calls nothing from
 * it (the DMA callbacks flow the other way: usart_if.c calls INTO the GNSS
 * driver). Empty by design. */
#ifndef USART_IF_H_STUB
#define USART_IF_H_STUB
#endif
